#include <stdio.h>

int main (){

float tempcel; 

printf("Digite a temperatura em Celsius: "); 
scanf("%f", &tempcel); 

float faren; 

faren = (float) (tempcel * 9.0 / 5.0) + 32; 

printf("Temperatura em Fahrenheit: %.2f\n", faren);

return 0; 

}